export async function api(path:string,data?:unknown,signal?:AbortSignal){const r=await fetch(path,{...(data?{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)}:{}),cache:'no-store',signal});const d:any=await r.json();if(!r.ok){const e=new Error(d.error||'请求未完成，请重试') as Error&{status:number;retryAfterMs?:number};e.status=r.status;if(typeof d.retryAfterMs==='number')e.retryAfterMs=d.retryAfterMs;throw e;}return d;}
